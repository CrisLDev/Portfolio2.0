import React, { Fragment } from 'react';
import * as Ui from '../../shared/Shared';

const Loader = () => {
    return (
        <Fragment>
            <Ui.CircularProgress />
        </Fragment>
    )
}

export default Loader
